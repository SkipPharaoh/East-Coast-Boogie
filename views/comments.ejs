<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Comments</title>
</head>
<body>
    <div>
        <h1>Comments Page</h1>
        <nav>
            <%- include('./partials/nav.ejs')%>
        </nav>
    </div>
    <section>
        <% for (let i = 0; i < comment.length; i++) { %>
            <a href="/comments/<%=comment[i]._id%>"><%= comment[i].name%></a>
            <p><%= comment[i].comment%></p>
            <p><%= comment[i].beenHereBefore%></p>
        <% } %>
    </section>
    <a href="/comments/new">Create A New Comment Here!</a>
</body>
</html>